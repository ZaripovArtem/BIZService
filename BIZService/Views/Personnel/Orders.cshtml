@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<BIZService.Models.Order>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" type="text/css" />

<table class="table">
    <thead>
        <tr>
            <th scope="col">Название услуги</th>
            <th scope="col">Имя</th>
            <th scope="col">Фамилия</th>
            <th scope="col">Номер телефона</th>
            <th scope="col">Мастер</th>
            <th scope="col"></th>
            <th scope="col"></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Service)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserSurname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ContactPhone)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Personnels.Name)
                    @Html.DisplayFor(modelItem => item.Personnels.Surname)
                </td>
                @*<td>
                    @using (Html.BeginForm("Edit", "Personnel"))
                      {
                        <a class="btn btn-outline-primary"
                           asp-action="Edit"
                           asp-route-id="@item.OrderID">Изменить</a>
                     }
                </td>
                <td>
                    @using (Html.BeginForm("Delete", "Personnel"))
                    {
                        <a class="btn btn-outline-danger"
                           asp-action="Delete"
                           asp-route-id="@item.OrderID">Удалить</a>
                    }
                </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderID">Delete</a>
            </td>
            </tr>
        </tbody>
    }
    </table>

